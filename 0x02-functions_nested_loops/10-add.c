#include "main.h"

/**
 * add - adds two integers and returns the result
 * @x: number to add
 * @y: number to add
 *
 * Return: sum
 */

int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
