#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer
 *@n: integer to check
 *
 * Return: 0 on success
 *
 */

int _abs(int n)
	{
	int absVal;

	absVal = abs(n);
	return (absVal);

	return (0);
	}
