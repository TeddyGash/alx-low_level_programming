#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: str to find length of
 *
 * Return: 0 on success
 */

int _strlen(char *s)
	{
	int l;

	l = strlen(s);

	return (l);
	}
